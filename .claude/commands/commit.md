创建一个新的提交，包含我们所有尚未提交的改动。
运行 `git status && git diff HEAD && git status --porcelain` 查看哪些文件尚未提交。
把未跟踪文件和已修改文件加入暂存区。

添加一个原子化的提交信息，内容应与本次工作相匹配。

加上一个能反映本次工作的标签，例如 `feat`、`fix`、`docs` 等。

## 输出后必须附加

在回复末尾给出「下一步可选流程」（2-3 项），示例：
1. 开 PR / 发起 Review
2. 部署或运行验证测试
3. 开始下一个功能计划（plan-feature）
